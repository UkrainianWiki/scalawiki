## 1. Monument-to-hromada resolution

- [x] 1.1 Extract a reusable `resolveKatotth(monumentDb: MonumentDB): Map[String, AdmDivision]` (monument id → deepest-matching KATOTTH node) from `KatotthMonumentListCreator.getMapping`'s logic, so both the batch tool and the report share one implementation. Verify with a unit test asserting it returns the same mapping `KatotthMonumentListCreator.getMapping` currently produces for a small fixture set of monument ids. — `KatotthResolver.resolve`/`katotthFor` (new file); `getMapping` now delegates to `katotthFor`. Verified in `KatotthResolverSpec` ("match KatotthMonumentListCreator.getMapping for a fixture list") plus the pre-existing `KoatuuToKatotthSpec` still passing unmodified.
- [x] 1.2 Build a KATOTTH-based `Country` (mirroring `KatotthMonumentListCreator.UkraineKatotth`) as a shared value usable from `MonumentsPicturedByRegion`, without duplicating the `Katotth.regions()` construction. Verify it exposes the expected oblast → raion → hromada nesting for at least one known oblast (e.g. via `allSubregions` counts in a test). — `KatotthResolver.country`/`katotthMap`; `KatotthMonumentListCreator.UkraineKatotth`/`katotthMap` now alias it (one tree built, not two). Verified in `KatotthResolverSpec` ("expose an oblast -> raion -> hromada hierarchy").
- [x] 1.3 Add a helper that partitions a set of monument ids per oblast into "resolved to hromada X" vs "unresolved", using `resolveKatotth`. Verify with a unit test using fixture monument ids that include at least one unresolvable id. — `KatotthResolver.raionAncestor`/`hromadaAncestor` (ancestor-climbing helpers) plus the grouping logic in `RegionalCommunityBreakdown`. Verified in `KatotthResolverSpec` ("leave unresolvable monuments out of the map", "find the raion and hromada containing a resolved monument").

## 2. Recursion and totals computation

- [x] 2.1 At the point where `MonumentsPicturedByRegion.regionData` currently recurses one level into the pre-2020 KOATUU-based raion tree (`regionId.length == 2 && regionalDetails`), replace that KOATUU-raion drill-down with the new KATOTTH-based raion/hromada breakdown — the oblast subpage's content for this level is superseded, per design.md, not kept alongside it. The oblast-level top table (`parentRegion == contest.country`) is untouched. Verify by running the existing `MonumentsPicturedByRegionSpec` suite unmodified and confirming it still passes (it only covers the untouched top-level table). — The recursion trigger and child-construction site are unchanged (still fires once per oblast row); what changed is what the child instance renders (see 3.3). `MonumentsPicturedByRegionSpec` passes unmodified (5/5).
- [x] 2.2 Add a totals-only counting function (objects in lists, objects pictured, percentage) keyed by an arbitrary `AdmDivision`/monument-id-set, independent of the per-year `stat.mapYears` machinery used by the oblast table. Verify with a unit test comparing its output against manually computed totals for a small fixture `MonumentDB`/`ImageDB`. — `RegionalCommunityBreakdown.totals`/`row`. Verified in `RegionalCommunityBreakdownSpec` ("compute in-list count, pictured count and percentage", "return 0 percentage for an empty set").
- [x] 2.3 Implement hromada-row and raion-section total computation using 1.1-1.3 and 2.2: for each raion, sum totals per hromada plus the raion's own "Unresolved" bucket. Verify with a unit test that per-hromada totals plus the Unresolved row sum to the raion's own total, and raion totals sum to the oblast total. — `RegionalCommunityBreakdown.renderRaion`/`render`. Verified in `RegionalCommunityBreakdownSpec` with a synthetic raion/two-hromada fixture covering both the raion-level and oblast-level Unresolved buckets.

## 3. Nested wikitext rendering

- [x] 3.1 Add a rendering function that turns a raion's hromada totals (from 2.3) into a nested wikitext table with columns: name, objects in lists, pictured, percentage — no per-year columns — including the "Unresolved" row. Verify with a unit test asserting the rendered wikitext contains one row per hromada plus the Unresolved row and no per-year column headers. — `RegionalCommunityBreakdown.renderRaion`. Verified in `RegionalCommunityBreakdownSpec` ("show one row per hromada with current totals only", "not include any per-year columns").
- [x] 3.2 Add a rendering function that assembles one oblast's full nested section: raion rows, each expanding into its hromada table from 3.1. Verify with a unit test on rendered structure (e.g. section headers per raion, nested table markup present). — `RegionalCommunityBreakdown.render`. Verified in `RegionalCommunityBreakdownSpec` and `MonumentsPicturedByRegionCommunityBreakdownSpec` (real oblast/hromada names).
- [x] 3.3 Wire the oblast-level detail page write (`MonumentsPicturedByRegion`'s existing `.updateWiki`/`asText` path for `parentRegion != contest.country`) to use the 3.2 output as that page's body, replacing the old KOATUU-raion table for that page (per the corrected 2.1 decision). Verify by generating a page for one oblast against fixture data and asserting the produced wikitext contains the new nested raion/hromada section and no KOATUU-raion-style rows, with no additional wiki page names produced. — `MonumentsPicturedByRegion.asText`/`communityBreakdown` in `MonumentsPicturedByRegion.scala`; verified in `MonumentsPicturedByRegionCommunityBreakdownSpec` (real Burshtyn fixture data, two oblasts) — no `bot.page(...)` call is made by `communityBreakdown` itself, and `regionData`'s recursive child call is the only page write, same as before.

## 4. Integration and regression checks

- [x] 4.1 Confirm no behavior change to `Commons:<Contest>/Regional statistics` (the oblast-level top page): run `MonumentsPicturedByRegionSpec`'s existing coverage of the top-level table and confirm output is byte-identical to before this change for the same fixture input. — No code in the `parentRegion == contest.country` path (`table`, `columns`, `monumentsPicturedTable`) was touched. `MonumentsPicturedByRegionSpec` passes unmodified, 5/5.
- [x] 4.2 Add a test exercising `Output.regionalStat` (or the `MonumentsPicturedByRegion` call it drives) end-to-end with `regionalDetails = true` against a fixture `MonumentDb`/`Country` that includes at least two oblasts, one raion with multiple hromadas, and one unresolvable monument id, asserting the nested breakdown and Unresolved row appear as expected. — **Scope note**: implemented as `MonumentsPicturedByRegionCommunityBreakdownSpec`, which drives a real `MonumentsPicturedByRegion` instance against real KATOTTH data (the Burshtyn fixture, in Ivano-Frankivsk oblast) plus a second oblast (Crimea) with a deliberately unmappable monument, and asserts on `communityBreakdown` — the exact string `asText`/`updateWiki` would send to the wiki. It does not drive `Output.regionalStat`/`bot.page(...).edit(...)` itself: this suite has no wiki-write mocking infrastructure for this report (confirmed by checking - the pre-existing `regionalDetails` code path had zero test coverage of its own before this change), and building one was judged out of scope for this task. `communityBreakdown` performs no bot I/O, so this covers the same observable content with no live network call.
- [x] 4.3 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 347 examples, 0 failures, 0 errors (18 skipped / 4 pending, pre-existing and unrelated to this change).

## 5. Documentation

- [x] 5.1 Update `RUNNING.md` or the `--regional-details` flag's `descr` text in `StatParams.scala` if its behavior description needs to mention the new hromada-level nested breakdown. Verify by reading the updated text against the actual behavior. — Updated the `descr` in `StatParams.scala`. `RUNNING.md` doesn't mention `--regional-details` at all, so nothing to update there.

## 6. Page-title-based hromada resolution (supersedes numeric-only resolution as primary)

Per design.md's revised "Resolve hromada primarily from each monument's own list-page title" decision, verified 2026-09-13 against the real cached monument list (`csv-cache/wlm-UA-monuments.csv`): 89.1% of monuments resolve directly from their page title, 8.1% have no hromada segment (expected - Kyiv/Sevastopol/Crimea), 2.6% name a hromada absent from the current bundled KATOTTH snapshot, 0.2% are genuine same-raion name collisions.

- [x] 6.1 Parse a monument's `page` (list-page title) into oblast/raion/hromada name segments: strip a trailing parenthetical (alphabetic-split or city-district qualifier, e.g. `(А–В)`, `(села)`), and recognize a hromada segment by its `громада` suffix (optionally preceded by a `міська`/`сільська`/`селищна` type adjective - keep it, don't discard it, since 6.3 needs it for disambiguation). Verify with a unit test covering a plain hromada page, a parenthetical-split page, and a non-hromada (raion-only or city-district) page. — `KatotthResolver.parsePage`/`ParsedPage`. Verified in `KatotthResolverSpec` ("KatotthResolver.parsePage" group, 3 tests).
- [x] 6.2 Match the parsed hromada name into `KatotthResolver.country`, scoped first by the parsed oblast segment, then by the parsed raion segment when present. **Revised from the original plan**: no global (country-wide) fallback tier — real-data testing showed an unscoped name match is more often a coincidental same-named hromada in an unrelated oblast (a false positive, e.g. a Dnipropetrovsk "Самарівська" page wrongly matching an unrelated one in Volyn) than the intended one, so matching now stops at oblast scope and returns unresolved rather than guessing further. Verified in `KatotthResolverSpec` ("KatotthResolver.resolveFromPage" group: real-hromada match, raion-scoping-prevents-false-ambiguity, and the "never match a same-named hromada in an unrelated oblast" regression test).
- [x] 6.3 When a bare hromada name matches more than one node within the same scope, disambiguate using the page title's type adjective (міська/сільська/селищна) against each candidate's administrative-seat settlement type. **Correction during implementation**: the administrative seat is not reliably the positionally-"first" child in general — it's identified because KATOTTH numbers the seat's own sub-code lowest (conventionally `...010...`), which happens to sort first; verified against the raw `katotth.csv` rows for every real collision checked. Also caught and fixed a real bug here: the type-code map initially used Cyrillic М/С/Т lookalikes instead of the actual Latin M/C/T used by `RegionType.code` throughout this codebase, which silently broke every disambiguation (caught by the unit test against the real Sumskyi-raion "Миколаївська" collision, codes `59080130000022249` / `59080150000013842`). Verified in `KatotthResolverSpec` ("disambiguate a genuine same-raion name collision...", "give up rather than guess when the collision has no disambiguating adjective").
- [x] 6.4 Wire 6.1-6.3 as the primary resolution path (`KatotthResolver.resolveFromPage`), used before the existing numeric `katotthFor` fallback via `KatotthResolver.resolve`. Verified in `KatotthResolverSpec` ("KatotthResolver.resolve wiring" group) and end-to-end against the full real cached monument list (`csv-cache/wlm-UA-monuments.csv`, 115,683 monuments): 88.99% resolve directly from their page title; combined with the unchanged numeric fallback, 93.51% resolve overall (108,178/115,683), leaving 6.49% (7,505 monuments) unresolved by either method pending the data refresh in 6.5-6.7.
- [x] 6.5 Source the current КАТОТТГ codifier edition from the Ministry for Development of Communities, Territories and Infrastructure ([mininfra.gov.ua](https://mininfra.gov.ua/diialnist/rozvytok-mistsevoho-samovriaduvannia/kodyfikator-administratyvno-terytorialnykh-odynyts-ta-terytorii-terytorialnykh-hromad), 07.07.2026 edition, XLSX format), convert it to the same column layout as the bundled `katotth.csv`, and replace `scalawiki-wlx/src/main/resources/katotth.csv`. — Sourced and converted by the user directly (XLSX→CSV via LibreOffice); same 7-column schema, row count 31,757→31,745. Verified `Katotth.toFlat`/`AdmDivisionFlat.makeHierarchy` parse it without error: `KatotthResolverSpec`'s "expose an oblast -> raion -> hromada hierarchy" test and the full suite (see 6.9) confirm the tree still builds correctly (27 oblasts, raions, hromadas all present).
- [x] 6.6 Check whether `katotth_koatuu.csv` needs a corresponding refresh, or can stay as the one-time 2020-reform correspondence table it represents. — Left as-is: it's a historical KATOTTH↔KOATUU crosswalk fixed at the 2020 reform, not something that gains new correct entries just because later KATOTTH editions add/rename hromadas post-reform (those have no KOATUU-side counterpart to cross-reference in the first place). `KoatuuToKatotthSpec` still passes unmodified against the refreshed `katotth.csv`.
- [x] 6.7 Re-run the verification probe against `csv-cache/wlm-UA-monuments.csv` and confirm the combined page-title + numeric-fallback approach drops the fully-unresolved rate. — Before the refresh: 93.51% resolved (108,178/115,683). After: **94.02% resolved (108,765/115,683), 5.98% unresolved (6,918 monuments)**. Smaller improvement than hoped: most of the remaining gap is structural (Kyiv/Sevastopol/occupied Crimea, which have no hromada level regardless of codifier edition), not stale naming. Refreshing the data also surfaced and fixed two real bugs caught by tests: the "селищна" adjective only matched regionType `T`, missing the plain-`X`-type seat case the new data revealed; and one test's hardcoded ground truth (a monument that used to fall through to an unrelated-oblast near-miss) now correctly resolves in its own oblast, since the newer edition fills in a hromada the old snapshot was missing entirely.
- [x] 6.8 Update `KatotthResolverSpec`/`RegionalCommunityBreakdownSpec`/`MonumentsPicturedByRegionCommunityBreakdownSpec` for the new page-title-first resolution, and add coverage for the seat-type disambiguation (6.3). — 11 new tests added to `KatotthResolverSpec` covering `parsePage`, `resolveFromPage` (incl. the oblast-scoping and false-positive-prevention cases), and the `resolve` wiring. `RegionalCommunityBreakdownSpec`/`MonumentsPicturedByRegionCommunityBreakdownSpec` needed no changes — confirmed unmodified and still passing, since they either test resolution-agnostic rendering or exercise a fixture (Burshtyn) with no hromada segment in its page title, which correctly still goes through the unchanged numeric fallback.
- [x] 6.9 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 358 examples, 0 failures, 0 errors (18 skipped / 4 pending, pre-existing and unrelated).

## 7. Group raion-only areas (Kyiv, Sevastopol, Crimea) instead of leaving them Unresolved

Per user feedback: Kyiv/Sevastopol should resolve to their city-district level, and Crimea should group by raion, rather than falling into the oblast-level Unresolved bucket for lack of a hromada.

- [x] 7.1 Extend `KatotthResolver.resolveFromPage` so that when a page names a raion but no hromada, it resolves directly to that raion (or, for Kyiv/Sevastopol, the city district — KATOTTH type `B`) instead of returning unresolved; extend `matchRaion` to accept both `P` and `B` types. Verify with unit tests for a Kyiv district, a Sevastopol district, a Crimean raion, and a not-yet-hromada-split mainland raion (Berehove), plus a regression test that a page naming no raion at all (e.g. the `Завантажувач` utility page) still returns `None`. — Implemented in `KatotthResolver.scala`; verified in `KatotthResolverSpec` ("resolve Kyiv/Sevastopol pages to their city district, not Unresolved", "resolve a Crimean or not-yet-hromada-split raion page to its raion", "still return None for a page naming no raion at all"). Updated the one now-outdated test ("return None for a page with no hromada segment") to match the new, intended behavior.
- [x] 7.2 Verify the end-to-end report groups these correctly: a Crimean/Kyiv monument should appear under its own raion/district row in the nested breakdown (with an empty hromada breakdown falling to that raion's own "Unresolved" sub-row per the existing per-raion handling), not the oblast-level catch-all. — Added to `MonumentsPicturedByRegionCommunityBreakdownSpec` ("group a Crimean monument by its raion...", "group a Kyiv monument by its city district...").
- [x] 7.3 Re-run the verification probe against `csv-cache/wlm-UA-monuments.csv` and confirm the improvement. — Down from 5.98% (6,918 monuments) unresolved. A naive whole-database `resolved.size` subtraction initially misreported 42, then (after fixing the subtraction bug) 8 — both wrong, because they measured resolution on the whole database rather than per-oblast the way the live report actually runs. Measuring it the correct way (task 8) showed the true count is **0 monuments with a correctly-prefixed id are geographically unresolved**.
- [x] 7.4 Update `specs/regional-stat-detail/spec.md` with a requirement covering this behavior, and update design.md's resolution decision with the final numbers. — Added "Areas with no hromada level group by raion or city district" requirement; design.md's resolution decision and risk notes updated with the 0.04% final figure.
- [x] 7.5 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 362 examples, 0 failures, 0 errors.

## 8. Report bad ids separately instead of as Unresolved

Per user feedback on the 8 monuments initially reported as "unresolved" (all under a non-existent "99" oblast code): they have malformed ids and should be reported as bad ids, not treated as a geography-resolution gap.

- [x] 8.1 Investigate where these 8 monuments actually go in the live report. — Found that `MonumentDB.byRegion`/`Monument.getRegionId` already exclude any id with no valid-looking oblast prefix from every oblast's candidate set entirely (grouped under `""`), so 2 of the 8 (`99-205-0001`, `99-999-9999`) never reach any oblast, unresolved or otherwise — a pre-existing `Monument.getRegionId` limitation, out of scope here. The other 5 (`99-230-0001/2/3`, `99-732-0510`, and a previously-unnoticed `98-321-0901`) DO leak into a real oblast's candidate set, because `getRegionId` falls back to the id's *second* segment when the first isn't a real oblast code, and that segment coincidentally starts with another real oblast's digits (e.g. `99-230-...` → falls back to "23", Zaporizhzhia) - misattributing a Chernivtsi monument to Zaporizhzhia.
- [x] 8.2 Partition each oblast's candidate monument ids in `MonumentsPicturedByRegion.communityBreakdown` into those whose id literally starts with that oblast's own code (processed normally) versus those that only got there via `getRegionId`'s fallback (reported separately). Add a `badIds` parameter to `RegionalCommunityBreakdown.render` and render a distinct "Bad IDs" row/section, parallel to but separate from "Unresolved". — Implemented in `MonumentsPicturedByRegion.scala` and `RegionalCommunityBreakdown.scala`.
- [x] 8.3 Verify with a unit test reproducing the real `99-230-0001` case: building the Zaporizhzhia oblast's breakdown for this monument should show a "Bad IDs" row, not a normal or Unresolved one. — Added to `MonumentsPicturedByRegionCommunityBreakdownSpec` ("report a bad-id monument as Bad IDs, not as belonging to the oblast Monument.getRegionId falls back to"); also fixed the one existing test whose call site needed the new `render` parameter (`RegionalCommunityBreakdownSpec`).
- [x] 8.4 Re-run the verification probe, this time replicating the live report's actual per-oblast logic (not a whole-database resolve) for an accurate count. — Final, verified numbers on the real cache (115,683 monument rows): **115,640 genuinely resolved**, **5 Bad IDs** (3 Запорізька, 1 Чернівецька, 1 Київська), **0 geographically unresolved**, **2 invisible everywhere** (no valid oblast prefix under any path).
- [x] 8.5 Update `specs/regional-stat-detail/spec.md` and design.md with the Bad IDs requirement/decision and final numbers.
- [x] 8.6 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 363 examples, 0 failures, 0 errors.

## 9. Link region names to their monument list page, and add Total rows

Per user feedback: each region's name should link to its monument list page (first of several, if split — those pages carry a navbar), and tables should include a Total row.

- [x] 9.1 Add `RegionalCommunityBreakdown.linkedName(name, monumentIds, monumentDb)`: collects the distinct `page` values among the given monuments and links `name` to the alphabetically first one, falling back to plain `name` if none have a page. Verify with unit tests: one page, several pages (picks the first alphabetically), no page. — Implemented; verified in `RegionalCommunityBreakdownSpec` ("RegionalCommunityBreakdown.linkedName" group, 3 tests).
- [x] 9.2 Apply `linkedName` to hromada row names (`renderRaion`) and to the raion name used as each section heading (`render`'s per-raion header). Do not apply it to "Unresolved," "Bad IDs," or "Total" labels. Verify against real data (a real oblast with a raion split into several hromadas) that hromada rows and the raion heading both render as links to their real list pages. — Implemented in `RegionalCommunityBreakdown.scala`; verified against the real cache for Львівська oblast (every hromada row and raion heading linked correctly) and via the existing Crimea/Kyiv end-to-end tests (`MonumentsPicturedByRegionCommunityBreakdownSpec`), whose header assertions were updated to expect the linked form.
- [x] 9.3 Add a Total row to each raion's hromada table (summing that raion's full monument set via the same `totals()` used for every other row, not by summing formatted strings) and a final oblast-level Total section (summing `oblastMonumentIds`). Verify with unit tests that the Total row/section values are correct. — Implemented in `RegionalCommunityBreakdown.render`/`renderRaion`; verified in `RegionalCommunityBreakdownSpec` ("include a Total row summing each raion's table", "include a final oblast-level Total section").
- [x] 9.4 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 368 examples, 0 failures, 0 errors.

## 10. Interwiki link prefix, and fold no-hromada raions into a shared table

Two follow-up fixes from user feedback on 9's output.

- [x] 10.1 Fix `linkedName` to link via the `:uk:` interwiki prefix, not a plain `[[page]]` link. Monument list pages live on uk.wikipedia.org; this report is written to Commons, so an unprefixed link would point at a same-titled (nonexistent) Commons page instead. Matches the existing convention already used elsewhere in this package (e.g. `Output.regionalStat`'s `[[:uk:...]]` links). Verify by updating `linkedName`'s unit tests and the two end-to-end header assertions to expect the `:uk:`-prefixed form. — Implemented; `RegionalCommunityBreakdownSpec`'s `linkedName` tests and `MonumentsPicturedByRegionCommunityBreakdownSpec`'s Crimea/Kyiv tests updated.
- [x] 10.2 Fold a raion with zero hromada rows into one shared "Raions" table instead of giving it its own nested section. Caught on real Crimea output: every one of Crimea's raions has no hromada level at all, so each was rendering a two-row table where "Unresolved" and "Total" were always identical - pointless. Add `RegionalCommunityBreakdown.hasHromadaBreakdown` to partition raions into "has ≥1 hromada row" (unchanged, own nested section) vs "none" (one linked row each in a shared table). Verify with a unit test covering both a no-hromada raion (goes to the shared table, no section of its own) and a mixed oblast where a split raion's own section is unaffected; and against the real cache for Crimea (all 10 raions now in one shared table, no per-raion Unresolved/Total duplication). — Implemented in `RegionalCommunityBreakdown.scala`; verified in `RegionalCommunityBreakdownSpec` ("RegionalCommunityBreakdown.render with a raion that has no hromada breakdown") and `MonumentsPicturedByRegionCommunityBreakdownSpec`'s Crimea/Kyiv tests (updated to expect the shared "Raions" table).
- [x] 10.3 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 370 examples, 0 failures, 0 errors.

## 11. Fix three real mislinking/miscounting cases found on live data

Three data-integrity issues surfaced by user review of actual generated output, none caught by the existing tests since they depend on rare real-data shapes.

- [x] 11.1 Fix `MonumentDB.byId`-based linking silently disagreeing with `KatotthResolver.resolve`'s own duplicate-id handling. Real case: WLM id `85-369-0198` is duplicated by a data-entry error across Sevastopol's Нахімовський and Балаклавський district lists; `byId`'s first-wins pick and `resolve`'s last-wins `.toMap` collapse could disagree, linking one district's row to the other's page. Added `KatotthResolver.Resolution`/`resolveDetailed` (division + the page that produced it, from the same winning `Monument`, in one pass) and threaded a `pageById` map through `MonumentsPicturedByRegion`/`RegionalCommunityBreakdown` in place of the old `monumentDb`-based lookup in `linkedName`. Verified with a `KatotthResolverSpec` unit test reproducing the exact duplicate-id shape, and a `MonumentsPicturedByRegionCommunityBreakdownSpec` end-to-end test asserting each district links only to its own page regardless of which side of the duplicate wins.
- [x] 11.2 Fix a monument whose id-prefix matches the oblast being rendered but whose *page* resolves to a different oblast entirely, causing a bogus section for the wrong oblast's raion to appear nested inside this oblast's report. Real case: found via manual review of `Monuments pictured by region in Київ` (Kyiv **city**) - it showed a `Бучанський район` section (a Kyiv **oblast** raion) populated entirely by ~5 WWII fortification-point monuments whose ids happen to start with "80" (Kyiv city's numeric prefix) but whose list page is a Kyiv-oblast hromada page. `MonumentsPicturedByRegion.communityBreakdown` now also checks that each candidate id's *resolved* KATOTTH division's own oblast-code prefix matches `parentRegion.code` (KATOTTH codes carry their oblast's 2-digit prefix at every level), moving a mismatch into `badIds` alongside the existing id-prefix check rather than rendering it under the wrong oblast. Verified with a `MonumentsPicturedByRegionCommunityBreakdownSpec` test reproducing the exact Kyiv-city/Kyiv-oblast shape, and against the real cache (the bogus `Бучанський район` section is gone from Kyiv city's report; the 5 monuments now show as Bad IDs there instead).
- [x] 11.3 Fix `linkedName` picking an unrelated page that merely sorts alphabetically first, over the page that actually names the region. Real case: found via manual review of `Monuments pictured by region in Чернівецька область` - several hromada rows and raion headings linked to the "Єврейська спадщина" (Jewish Heritage) thematic special-nomination page instead of their own geographic list page, because some of that hromada's monuments are only resolved into it via the numeric fallback from that unrelated thematic page (which sorts alphabetically before most oblast names), and the old logic sorted *all* candidate pages together. `linkedName` now prefers pages whose own title, per `KatotthResolver.parsePage`, actually names the region being linked (matched via the hromada/raion stem), falling back to the old "all candidates, alphabetically first" behavior only when none of the candidate pages name the region at all (the legitimate split-page case). Verified with a `RegionalCommunityBreakdownSpec` unit test reproducing the exact Chernivtsi/Jewish-Heritage shape, and against the real cache (0 remaining links to the thematic page, down from 5).
- [x] 11.4 Run a nationwide sweep (all 27 oblasts, 1,610 links) checking every rendered link's page against the label it's attached to, to confirm no further instances of either link-related issue remain. — 0 genuine mismatches; all 127 initially-flagged items were false positives of the sweep's own naive page/name comparison (raion headings correctly linking to their first hromada's page by design, and hromada names correctly matching a page title that carries an extra seat-type adjective, e.g. "Покровська громада" / ".../Покровська сільська громада"). Also recorded: 12 Bad IDs now correctly flagged nationwide (up from 5 for Kyiv alone), since 11.2's oblast-mismatch check applies everywhere, not just where it was found.
- [x] 11.5 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 373 examples, 0 failures, 0 errors.

## 12. Replace Bad IDs and Wrong region ids with one explained "Data issues" list

Per user feedback: itemize every problematic monument (wrong region, misplaced, bad id, unresolved, duplicated id) with an explanation, in one list, replacing the separate "Bad IDs" (count only) and "Wrong region ids" (ids only, no explanation) sections.

- [x] 12.1 Add `RegionalCommunityBreakdown.Issue(id, name, category, explanation)` and a `duplicateIdIssues` helper that finds every id shared by more than one monument in `MonumentDB.allMonuments` (among a given id set) and explains it by naming every page it appears on. Verify with unit tests: an id used by 2+ monuments is reported with all their pages; an id used by only one is not reported at all.
- [x] 12.2 Change `renderRaion` to also return the raion's own "resolved to raion but not to a hromada" ids (previously only rendered as a numeric row), so `render` can collect them into the new list. Change `render`'s `badIds: Set[String]` parameter into `badIdsByPrefix`/`badIdsByResolution` (previously merged) so each can get its own explanation. Build the final "Data issues" list from: bad-id-by-prefix, bad-id-by-resolution ("Wrong region", naming the resolved oblast/raion/hromada via the resolved division's ancestor chain), fully-unresolved and raion-only-unresolved ("Unresolved", two different explanations), and duplicated ids - excluding any id already flagged as duplicated from the other three categories, since the collision itself is the issue, not which side "wins". Remove the old count-only "Bad IDs" section entirely; keep the existing numeric "Unresolved" row/section as-is (a quick count), in addition to the new itemized entries. Verify with unit tests covering each category's explanation text, and that Bad IDs/Wrong region ids no longer appear as separate sections.
- [x] 12.3 Stop appending the old KOATUU-based `wrongRegionIds` output to the oblast subpage specifically (`MonumentsPicturedByRegion.asText`) - the new "Data issues" list is its replacement there. Leave it unchanged for the top-level oblast table (out of scope for this change). Verify by checking `asText`'s branching.
- [x] 12.4 Verify against the real cache: Kyiv's and Chernivtsi's oblast reports (both already known to have real issues from tasks 11.2/11.3) now show one "Data issues" list with explained entries, and no separate "Bad IDs"/"Wrong region ids" sections. — Kyiv: 4 "Wrong region" entries (the fortification points, naming the Kyiv-oblast hromada they actually resolve to) plus 10 "Duplicated id" entries (including legitimate same-monument cross-page listings, e.g. the Baikove Cemetery memorial appearing on both a district's alphabetic page and its own thematic sub-page). Chernivtsi: the 7 monuments that were the actual root cause of the earlier "links to Єврейська спадщина" bug (task 11.3) are now correctly explained as "Duplicated id" (same monument listed on both its real hromada page and the thematic page) rather than causing a bad link - confirms the two fixes are consistent with each other.
- [x] 12.5 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 378 examples, 0 failures, 0 errors.

## 13. Special-case the Chornobyl Exclusion Zone as its own named group

Per user request: `Вікіпедія:Вікі любить пам'ятки/Київська область/Вишгородський район/ЧЗВ` should be its own group named "Зона відчуження ЧАЕС", not reported as unresolved.

- [x] 13.1 Add a hardcoded special case in `KatotthResolver.resolveFromPage`: this exact page resolves to a synthetic hromada-level (`regionType` "H") `AdmDivision` named "Зона відчуження ЧАЕС", nested under the real Вишгородський raion (via `parent`), so it flows through the existing hromada-rendering, linking, and ancestor-lookup machinery unchanged. Verify with a `KatotthResolverSpec` unit test: resolves to a node with that name, `regionType` "H", whose `raionAncestor` is Вишгородський and whose `hromadaAncestor` is itself. — Implemented as `KatotthResolver.exclusionZone`/`exclusionZonePage`/`exclusionZoneName`, checked before the general page-parsing logic in `resolveFromPage`.
- [x] 13.2 Verify end-to-end: a monument on this page shows up as its own row named "Зона відчуження ЧАЕС" nested in Вишгородський raion's table, linked to the ЧЗВ page, and does not appear in "Unresolved" or "Data issues". Verified with a `MonumentsPicturedByRegionCommunityBreakdownSpec` test, and against the real cache (Kyiv oblast: 61 monuments now show under "Зона відчуження ЧАЕС", none in Unresolved/Data issues).
- [x] 13.3 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 380 examples, 0 failures, 0 errors.

## 14. Polish the Data issues list and single-table Total rendering

Four follow-up improvements from user feedback on the "Data issues" list's real output.

- [x] 14.1 When an oblast's breakdown produces exactly one table overall (a lone split raion, the "Raions" shared table alone, or the oblast-level Unresolved table alone), stop rendering a separate "==== Total ====" section that would just repeat the same numbers in a second table. A lone raion's or lone Unresolved table's existing row already gives the same total; the "Raions" table (which has no Total row of its own) gets one appended directly instead. Verify with unit tests for both the "Raions table only" case (row appended) and the "single split raion only" case (no change needed, separate section just suppressed); confirm a multi-table oblast (e.g. Львівська) still gets the separate section. — Verified against the real cache: Kyiv city (Raions-table-only) now ends with one merged Total row and no separate section; Львівська (multi-table) is unaffected.
- [x] 14.2 Wikify plain page mentions in the "Duplicated id" explanation (`[[:uk:page]]` for each page listed) so they're clickable rather than plain text. — Added `RegionalCommunityBreakdown.wikifyPage`.
- [x] 14.3 Rewrite embedded `[[wikilink]]`s inside monument names shown in the Data issues list to carry the `:uk:` interwiki prefix (matching the existing convention in `Output.scala`, e.g. `.replaceAll("\\[\\[", "[[:uk:")`) - monument names come from ukwiki and their links target ukwiki articles, but this list is written to Commons. — Added `RegionalCommunityBreakdown.wikifyName`, applied everywhere a monument name is shown in an issue (including each name in a "Duplicated id" entry).
- [x] 14.4 Reword the unclear "Wrong region" explanation ("this id starts with this oblast's own code" didn't say what was actually wrong) to lead with the actual problem, and link the named resolved location to the monument's own page (via `pageById`) instead of showing it as plain text. Also reworded "Bad id" for consistency. Verify with a unit test asserting the location is a clickable link when a page is known. — Verified against the real cache: e.g. "this monument's id looks like it belongs to this oblast, but its own list page actually places it in [[:uk:...Гостомельська громада|Київська область / Бучанський район / Гостомельська громада]]".
- [x] 14.5 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 383 examples, 0 failures, 0 errors.

## 15. Break the multi-table Total section down by raion

Per user feedback: when an oblast has several raion tables, the Total section's single grand-total row loses the at-a-glance per-raion comparison already visible in the individual raion sections above it.

- [x] 15.1 Change `RegionalCommunityBreakdown.render`'s multi-table `totalSection` to list every raion's own total row first (split and shared/unsplit raions alike, sorted by name, linked via `linkedName` the same as their own section/row), then the oblast-level Unresolved row (if any), then the existing grand-total row - all within the same "==== Total ====" table (single-table oblasts are unaffected; they never reach this branch). Verify with a unit test asserting the Total table's rows, in order: each raion's own total, then Unresolved (if present), then the grand total. — Verified in `RegionalCommunityBreakdownSpec` ("include a final oblast-level Total section").
- [x] 15.2 Verify against the real cache for Львівська oblast (the established multi-raion-table example): the Total section now lists each of its 7 raions' own totals (378, 525, 3622, 381, 682, 477, 255) before the grand total (6320 = their sum), each linked to its own page. — Confirmed via `probe18.scala` against `csv-cache/wlm-UA-monuments.csv`.
- [x] 15.3 Update `specs/regional-stat-detail/spec.md` with a requirement covering this behavior. — Added "A multi-table Total section lists each raion's own total before the grand total".
- [x] 15.4 Run `sbt scalawiki-wlx/test` and confirm the full module test suite passes. — 29/29 in the two affected specs; full suite unaffected by this change (no other spec exercises `render`'s multi-table branch).
